name: Cypress Cloud

on:
  workflow_dispatch:
    inputs:
      spec:
        description: Run cypress command line
        default: cypress/e2e/*

jobs:
 cypress-e2e-electron:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:node18.12.0-chrome103-ff107
      options: --user 1001
    steps:
      - name: Get code
        uses: actions/checkout@v6.0.2

      - name: Cypress run
        uses: cypress-io/github-action@v6.5.0
        with:
          install-command: yarn install
          browser: electron
          record: true
          group: UI Electron
          spec: ${{ github.event.inputs.spec }}
        env:
          CYPRESS_RECORD_KEY: 3b4605cd-5079-41ab-87df-c1366aab0d96
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
 cypress-e2e-chrome:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:node18.12.0-chrome103-ff107
      options: --user 1001
    steps:
      - name: Get code
        uses: actions/checkout@v6.0.2

      - name: Cypress run
        uses: cypress-io/github-action@v6.5.0
        with:
          install-command: yarn install
          browser: chrome
          record: true
          group: UI chrome
          spec: ${{ github.event.inputs.spec }}
        env:
          CYPRESS_RECORD_KEY: 3b4605cd-5079-41ab-87df-c1366aab0d96
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
 cypress-e2e-firefox:
    runs-on: ubuntu-latest
    container:
      image: cypress/browsers:node18.12.0-chrome103-ff107
      options: --user 1001    
    steps:
      - name: Get code
        uses: actions/checkout@v6.0.2

      - name: Cypress run
        uses: cypress-io/github-action@v6.5.0
        with:
          install-command: yarn install
          browser: firefox
          record: true
          group: UI firefox
          spec: ${{ github.event.inputs.spec }}
        env:
          CYPRESS_RECORD_KEY: 3b4605cd-5079-41ab-87df-c1366aab0d96
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

        
        
